# 这个文件是模板文件，供其他开发者拉取代码后参考
# 他们需要复制这个文件，重命名为 application-dev.yml，并填入自己的数据库账号密码
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cas_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: YOUR_LOCAL_MYSQL_PASSWORD # 你的数据库密码
